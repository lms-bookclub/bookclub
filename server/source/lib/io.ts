import express from 'express';
// import * as cookies from 'socket.io-cookie-parser';
//
const server = express();
// import http from 'http'.Server(server);
// const io = require('socket.io')(http, {});
//
// io.use(cookies());

export {
  server,
  // io,
  // http
}